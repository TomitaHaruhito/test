name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Run a one-line script
      run: echo Hello, world!
    - name: Run a multi-line script
      run: ssh　-o 'StrictHostKeyChecking no' -i "${{ secrets.SSH_KEY }}" ec2-user@ec2-18-179-36-225.ap-northeast-1.compute.amazonaws.com "touch data2.txt"
